Write responses, documentation, and new comments in English. `core` is the single source of truth for migrated content; `scripts/build.py` generates `skills` and `dist`. `vendor` preserves the fixed upstream snapshot for provenance and is not loaded as active instructions. After changes, run `python3 -m unittest discover -s tests -v` and `python3 scripts/build.py --check`. Run model behavior tests only in isolated directories. Do not change global configuration, send messages, or publish as part of those tests.
